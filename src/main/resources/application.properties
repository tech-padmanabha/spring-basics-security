spring.application.name=spring-basics

server.port=8888

#Eureka
#eureka.client.service-url.defaultZone=http://localhost:8761/eureka/


#datasource
spring.datasource.url=jdbc:mysql://localhost:3306/user_auth?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=sahu


#log JPA queries (should comment at production)
spring.jpa.show-sql=true


# Drop and create new tables (create, create-drop, validate, update)
# Only for testing purposes - comment this in production
spring.jpa.hibernate.ddl-auto=create


#application-config
white.list.url=/api/**
auth.list.url=/auth/**
